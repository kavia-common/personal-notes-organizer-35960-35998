import React from 'react';
import './App.css';

/**
 * PUBLIC_INTERFACE
 * App is a minimal container kept for compatibility with tests; actual routing is in AppRouter.
 */
export default function App() {
  return (
    <div className="App">
      <header className="App-header">
        <p>Personal Notes App</p>
      </header>
    </div>
  );
}
